# 08-技术决策

## 概述

本文档记录 HiSi DevTool Frontend 的技术选型决策，包括前端框架、状态管理、可视化方案、测试框架等关键技术决策。

---

## 决策记录

### ADR-001: 前端框架选择 Vue 3

**状态**：已采纳

**背景**：
需要选择一个现代前端框架来构建 HiSi DevTool Frontend。

**决策**：
选择 Vue 3 作为前端框架。

**理由**：
1. **学习曲线平缓**：Vue 3 的 Composition API 提供了更好的代码组织方式，同时保持了较低的学习门槛
2. **生态系统成熟**：Element Plus、Vue Router、Pinia 等配套库质量高
3. **TypeScript 支持**：Vue 3 原生支持 TypeScript，类型推导完善
4. **性能优秀**：Vue 3 的响应式系统和虚拟 DOM 性能优异
5. **社区活跃**：Vue 社区活跃，文档完善

**备选方案**：
| 方案 | 优点 | 缺点 |
|------|------|------|
| React | 生态最强大，社区最大 | 学习曲线较陡，需要选择状态管理方案 |
| Angular | 企业级框架，功能完整 | 学习曲线陡峭，框架较重 |
| Svelte | 性能极佳，编译时优化 | 生态相对较小，企业采用率低 |

**后果**：
- 使用 Vue 3 + TypeScript 作为核心技术栈
- 使用 Pinia 进行状态管理
- 使用 Vue Router 进行路由管理

---

### ADR-002: 状态管理选择 Pinia

**状态**：已采纳

**背景**：
Vue 3 需要一个状态管理方案来管理全局状态。

**决策**：
选择 Pinia 作为状态管理库。

**理由**：
1. **官方推荐**：Pinia 是 Vue 3 官方推荐的状态管理库
2. **TypeScript 友好**：原生支持 TypeScript，类型推导完善
3. **API 简洁**：相比 Vuex，API 更简洁直观
4. **DevTools 支持**：集成 Vue DevTools，调试方便
5. **模块化**：支持模块化组织，易于维护

**备选方案**：
| 方案 | 优点 | 缺点 |
|------|------|------|
| Vuex | 成熟稳定，文档完善 | API 相对复杂，TypeScript 支持较弱 |
| 自研方案 | 完全可控 | 开发成本高，缺乏生态支持 |

**后果**：
- 使用 `defineStore` 定义 Store
- 使用 Setup Store 语法（更接近 Composition API）
- Store 按功能模块组织

---

### ADR-003: UI 组件库选择 Element Plus

**状态**：已采纳

**背景**：
需要一个高质量的 UI 组件库来快速构建界面。

**决策**：
选择 Element Plus 作为 UI 组件库。

**理由**：
1. **Vue 3 原生支持**：Element Plus 专为 Vue 3 设计
2. **组件丰富**：提供了 50+ 高质量组件
3. **文档完善**：中文文档完善，示例丰富
4. **企业级**：适合企业级应用开发
5. **主题定制**：支持主题定制

**备选方案**：
| 方案 | 优点 | 缺点 |
|------|------|------|
| Ant Design Vue | 设计规范统一 | 组件数量相对较少 |
| Vuetify | Material Design 风格 | 包体积较大 |
| Naive UI | 性能优异，TypeScript 友好 | 生态相对较小 |

**后果**：
- 使用 Element Plus 组件构建界面
- 使用 Element Plus 的主题系统进行样式定制
- 使用 ElMessage 进行消息提示

---

### ADR-004: 可视化方案选择 ECharts + Mermaid + dagre

**状态**：已采纳

**背景**：
HiSi DevTool 需要多种可视化能力：数据图表、流程图、DAG 布局。

**决策**：
采用 ECharts + Mermaid + dagre 的组合方案。

**理由**：
1. **ECharts**：功能强大的数据可视化库，支持图表类型丰富
2. **Mermaid**：轻量级流程图/时序图库，使用文本描述图表
3. **dagre**：专业的 DAG 布局算法库，适合调用链可视化

**使用场景**：
| 库 | 使用场景 |
|---|---------|
| ECharts | 数据图表、桑基图、统计图表 |
| Mermaid | 流程图、时序图、架构图 |
| dagre | DAG 布局、调用链可视化 |
| Vue Flow | 交互式流程图 |
| Cytoscape | 图数据库可视化 |

**备选方案**：
| 方案 | 优点 | 缺点 |
|------|------|------|
| D3.js | 极其灵活 | 学习曲线陡峭，开发成本高 |
| Chart.js | 轻量级 | 功能相对有限 |
| G6 (AntV) | 图可视化专业 | 生态相对较小 |

**后果**：
- 使用 ECharts 渲染数据图表
- 使用 Mermaid 渲染流程图
- 使用 dagre 进行 DAG 布局
- 使用 Vue Flow 渲染交互式流程图
- 使用 Cytoscape 渲染图数据库数据

---

### ADR-005: HTTP 客户端选择 Axios

**状态**：已采纳

**背景**：
需要一个 HTTP 客户端库来与后端 API 通信。

**决策**：
选择 Axios 作为 HTTP 客户端。

**理由**：
1. **功能完善**：支持拦截器、取消请求、自动转换等
2. **浏览器兼容**：兼容所有现代浏览器
3. **TypeScript 支持**：原生支持 TypeScript
4. **社区活跃**：社区活跃，文档完善
5. **广泛使用**：被大量企业级项目采用

**备选方案**：
| 方案 | 优点 | 缺点 |
|------|------|------|
| Fetch API | 原生支持，无需依赖 | 功能相对简单，需要手动处理错误 |
| ky | 轻量级，API 简洁 | 生态相对较小 |
| got | 功能强大 | 主要用于 Node.js |

**后果**：
- 使用 Axios 发送 HTTP 请求
- 使用拦截器统一处理请求/响应
- 使用 Axios 的类型定义进行类型检查

---

### ADR-006: 测试框架选择 Vitest + Playwright

**状态**：已采纳

**背景**：
需要选择测试框架来保证代码质量。

**决策**：
采用 Vitest（单元测试）+ Playwright（E2E 测试）的组合方案。

**理由**：
1. **Vitest**：
   - 与 Vite 深度集成，速度快
   - 兼容 Jest API，迁移成本低
   - 原生支持 TypeScript 和 ESM

2. **Playwright**：
   - 支持多浏览器测试
   - API 简洁，易于使用
   - 支持录制和代码生成

**备选方案**：
| 方案 | 优点 | 缺点 |
|------|------|------|
| Jest | 成熟稳定，社区大 | 与 Vite 集成需要额外配置 |
| Cypress | E2E 测试专业 | 不支持多浏览器，价格较高 |
| Testing Library | 测试理念先进 | 需要配合其他测试运行器 |

**后果**：
- 使用 Vitest 进行单元测试
- 使用 Playwright 进行 E2E 测试
- 使用 happy-dom 模拟 DOM 环境

---

### ADR-007: 构建工具选择 Vite

**状态**：已采纳

**背景**：
需要选择一个现代构建工具来构建前端项目。

**决策**：
选择 Vite 作为构建工具。

**理由**：
1. **开发体验优秀**：冷启动快，热更新快
2. **原生 ESM 支持**：使用浏览器原生 ESM，无需打包
3. **插件生态**：插件生态丰富，易于扩展
4. **Vue 官方推荐**：Vue 3 官方推荐的构建工具
5. **配置简单**：配置简洁，易于上手

**备选方案**：
| 方案 | 优点 | 缺点 |
|------|------|------|
| Webpack | 成熟稳定，生态完善 | 配置复杂，构建速度慢 |
| Rollup | 打包质量高 | 开发体验一般 |
| esbuild | 极速构建 | 功能相对有限 |

**后果**：
- 使用 Vite 作为开发服务器和构建工具
- 使用 Vite 的代理功能进行 API 代理
- 使用 Vite 的插件系统进行功能扩展

---

### ADR-008: 终端模拟选择 xterm.js

**状态**：已采纳

**背景**：
HiSi DevTool 需要在浏览器中模拟终端，与 Claude CLI 交互。

**决策**：
选择 xterm.js 作为终端模拟库。

**理由**：
1. **功能完整**：支持完整的终端功能
2. **性能优秀**：渲染性能优异，支持大量输出
3. **可扩展**：支持插件扩展
4. **广泛使用**：VS Code 等知名项目使用
5. **活跃维护**：社区活跃，持续更新

**备选方案**：
| 方案 | 优点 | 缺点 |
|------|------|------|
| 自研方案 | 完全可控 | 开发成本高，功能有限 |
| terminal.js | 轻量级 | 功能相对简单 |

**后果**：
- 使用 xterm.js 创建终端实例
- 使用 @xterm/addon-fit 进行终端自适应
- 使用 @xterm/addon-web-links 支持链接点击

---

### ADR-009: Markdown 渲染选择 marked

**状态**：已采纳

**背景**：
HiSi DevTool 需要在浏览器中渲染 Markdown 内容。

**决策**：
选择 marked 作为 Markdown 渲染库。

**理由**：
1. **轻量级**：包体积小，性能好
2. **功能完善**：支持标准 Markdown 语法
3. **可扩展**：支持自定义渲染器
4. **广泛使用**：被大量项目采用
5. **活跃维护**：社区活跃，持续更新

**备选方案**：
| 方案 | 优点 | 缺点 |
|------|------|------|
| markdown-it | 功能强大，插件丰富 | 包体积较大 |
| showdown | 双向转换 | 性能一般 |
| remark | AST 处理强大 | 学习曲线陡峭 |

**后果**：
- 使用 marked 渲染 Markdown 内容
- 使用 marked 的自定义渲染器进行样式定制

---

### ADR-010: 实时通信选择 SSE + WebSocket

**状态**：已采纳

**背景**：
HiSi DevTool 需要实时通信能力，包括进度推送和双向交互。

**决策**：
采用 SSE（Server-Sent Events）+ WebSocket 的组合方案。

**理由**：
1. **SSE**：
   - 单向通信，适合进度推送
   - 基于 HTTP，兼容性好
   - 自动重连

2. **WebSocket**：
   - 双向通信，适合终端交互
   - 性能优异
   - 支持二进制数据

**使用场景**：
| 技术 | 使用场景 |
|------|---------|
| SSE | RAM 分析进度、合并分析进度 |
| WebSocket | Claude 终端、实时日志 |

**备选方案**：
| 方案 | 优点 | 缺点 |
|------|------|------|
| 长轮询 | 兼容性好 | 性能差，延迟高 |
| Socket.IO | 功能丰富 | 包体积大，协议复杂 |

**后果**：
- 使用 EventSource 接收 SSE 事件
- 使用 WebSocket 进行双向通信
- 使用自定义 Composable 封装连接逻辑

---

## 技术选型总结

### 核心技术栈

| 类别 | 技术 | 版本 |
|------|------|------|
| 前端框架 | Vue 3 | ^3.5.30 |
| 类型系统 | TypeScript | ~5.9.3 |
| UI 组件库 | Element Plus | ^2.13.5 |
| 状态管理 | Pinia | ^3.0.4 |
| 路由 | Vue Router | ^4.6.4 |
| HTTP 客户端 | Axios | ^1.13.6 |
| 构建工具 | Vite | ^8.0.0 |
| 单元测试 | Vitest | ^4.1.7 |
| E2E 测试 | Playwright | ^1.58.2 |

### 可视化技术栈

| 类别 | 技术 | 用途 |
|------|------|------|
| 数据图表 | ECharts 6 | 统计图表、桑基图 |
| 流程图 | Mermaid 11 | 流程图、时序图 |
| DAG 布局 | dagre | 调用链布局 |
| 交互式流程图 | Vue Flow | 交互式 DAG |
| 图数据库可视化 | Cytoscape | 图数据可视化 |

### 其他技术栈

| 类别 | 技术 | 用途 |
|------|------|------|
| 终端模拟 | xterm.js | Web 终端 |
| Markdown 渲染 | marked | Markdown 渲染 |
| 工具库 | lodash-es | 工具函数 |
| 实时通信 | SSE + WebSocket | 进度推送、终端交互 |

---

## 下一步

- [项目概览](../01-项目概览/index.md) - 了解项目整体情况
- [架构设计](../02-架构设计/index.md) - 了解系统架构
- [术语表](../09-术语表/index.md) - 了解项目术语

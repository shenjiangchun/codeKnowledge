# 远端 main 漏改/错改修复清单

> **基线对比**：`origin/main` (996c8b1) vs `feat/exception-auto-fix-and-codegraph` (7c79b1c)
> **生成时间**：2026-07-15
> **结论**：远端 main 拉取了 25 文件中的 18 文件改动（业务代码 17 + LogQuery 错改），遗漏 .gitignore + ProjectLanguageDetector 关键 114 行 + 7 个测试文件，LogQuery.vue 注释位置错放。

---

## 一、致命漏改（必须立即补上）

### 🔴 1. ProjectLanguageDetector.java 漏改 114 行 TS/JS 探测逻辑

**文件**：`hisi-dev-tool/src/main/java/com/huawei/hisi/knowledgegraph/util/ProjectLanguageDetector.java`

**现状**：
- 远端 main 版本：**132 行**（只在 enum 加了 `TYPESCRIPT, JAVASCRIPT` 两个字段，**detectLanguage 根本不会返回这两个值**）
- feat 分支版本：**236 行**（含 `hasTypeScriptMarkers` + `hasJavaScriptMarkers` + `countSourceFiles` TS/JS 计数）

**影响**：
- TS/JS 项目永远识别为 JAVA → KnowledgeGraphBuilder 的 TS 分流路径永远走不到
- CodegraphSidecarService 永远不会被触发 → TypeScript 图谱生成能力失效
- **整个 codegraph 集成在远端形同虚设**

**修复方法**：直接用 feat 分支版本覆盖远端版本

```bash
# 在远端执行：
git checkout feat/exception-auto-fix-and-codegraph -- hisi-dev-tool/src/main/java/com/huawei/hisi/knowledgegraph/util/ProjectLanguageDetector.java
git commit -m "fix(knowledgegraph): complete TS/JS marker detection in ProjectLanguageDetector (missed in previous commit)"
```

**完整正确内容**（feat 分支版本，236 行）：

```java
package com.huawei.hisi.knowledgegraph.util;

import lombok.extern.slf4j.Slf4j;

import java.nio.file.Files;
import java.nio.file.Path;
import java.nio.file.Paths;
import java.util.Set;
import java.util.stream.Collectors;
import java.util.stream.Stream;

/**
 * Utility to detect the primary programming language of a project.
 * Used by KnowledgeGraphBuilder to decide whether to build Java, Python,
 * or TypeScript (via codegraph sidecar) knowledge graph.
 */
@Slf4j
public final class ProjectLanguageDetector {

    private ProjectLanguageDetector() {
        // utility class
    }

    /**
     * Language enum - JAVA / PYTHON handled by hisi native scanners;
     * TYPESCRIPT / JAVASCRIPT (含 Vue) 由 codegraph sidecar 处理。
     */
    public enum Language {
        JAVA,
        PYTHON,
        TYPESCRIPT,
        JAVASCRIPT
    }

    /**
     * Detect the primary language of a project by checking for language-specific
     * project files and counting source files.
     *
     * @param projectPath path to project root
     * @return detected language (defaults to JAVA if no clear signal)
     */
    public static Language detectLanguage(String projectPath) {
        Path root = Paths.get(projectPath);
        if (!Files.exists(root)) {
            log.warn("[LanguageDetector] Project path does not exist: {}, defaulting to JAVA", projectPath);
            return Language.JAVA;
        }

        // 1. Check for Java-specific project markers
        if (hasJavaMarkers(root)) {
            log.info("[LanguageDetector] Detected Java project (pom.xml/build.gradle found)");
            return Language.JAVA;
        }

        // 2. Check for Python-specific project markers
        if (hasPythonMarkers(root)) {
            log.info("[LanguageDetector] Detected Python project (requirements.txt/pyproject.toml/setup.py found)");
            return Language.PYTHON;
        }

        // 3. Check for TypeScript/Vue markers (Vue 视为 TypeScript)
        if (hasTypeScriptMarkers(root)) {
            log.info("[LanguageDetector] Detected TypeScript/Vue project (tsconfig.json/vue.config.js/nuxt.config.ts or .ts/.tsx/.vue files)");
            return Language.TYPESCRIPT;
        }

        // 4. Check for JavaScript markers
        if (hasJavaScriptMarkers(root)) {
            log.info("[LanguageDetector] Detected JavaScript project (package.json without tsconfig.json or .js/.jsx files)");
            return Language.JAVASCRIPT;
        }

        // 5. Count source files to decide
        Language counted = countSourceFiles(root);
        log.info("[LanguageDetector] Language decided by source file count: {}", counted);
        return counted;
    }

    /**
     * Check if the project has Java-specific marker files.
     */
    private static boolean hasJavaMarkers(Path root) {
        return Files.exists(root.resolve("pom.xml"))
            || Files.exists(root.resolve("build.gradle"))
            || Files.exists(root.resolve("build.gradle.kts"))
            || Files.exists(root.resolve("src/main/java"));
    }

    /**
     * Check if the project has Python-specific marker files.
     */
    private static boolean hasPythonMarkers(Path root) {
        return Files.exists(root.resolve("requirements.txt"))
            || Files.exists(root.resolve("pyproject.toml"))
            || Files.exists(root.resolve("setup.py"))
            || Files.exists(root.resolve("setup.cfg"))
            || Files.exists(root.resolve("Pipfile"))
            || Files.exists(root.resolve("Pipfile.lock"));
    }

    /**
     * Check if the project has TypeScript/Vue marker files.
     *
     * <p>判定条件（满足任一即视为 TS 项目，由 codegraph sidecar 处理）：
     * <ul>
     *   <li>根目录存在 {@code tsconfig.json}</li>
     *   <li>根目录存在 {@code vue.config.js} 或 {@code nuxt.config.ts}（Vue 视为 TS）</li>
     *   <li>根目录前 1000 个非排除目录文件中存在 {@code .ts/.tsx/.vue} 文件</li>
     * </ul>
     * 排除目录沿用 {@link com.huawei.hisi.service.CodeAnalysisCoreService#EXCLUDED_SCAN_DIRS}，
     * 避免 {@code node_modules} 等目录里的第三方 .ts 文件造成误判。</p>
     */
    private static boolean hasTypeScriptMarkers(Path root) {
        if (Files.exists(root.resolve("tsconfig.json"))) {
            return true;
        }
        // Vue 项目（vue.config.js / nuxt.config.ts）视为 TypeScript
        if (Files.exists(root.resolve("vue.config.js"))
                || Files.exists(root.resolve("nuxt.config.ts"))) {
            return true;
        }
        try (Stream<Path> walk = Files.walk(root, 10)) {
            return walk
                .filter(Files::isRegularFile)
                .filter(p -> {
                    for (Path seg : p) {
                        if (com.huawei.hisi.service.CodeAnalysisCoreService
                                .EXCLUDED_SCAN_DIRS.contains(seg.toString())) {
                            return false;
                        }
                    }
                    return true;
                })
                .limit(1000)
                .anyMatch(p -> {
                    String name = p.getFileName().toString();
                    return name.endsWith(".ts") || name.endsWith(".tsx") || name.endsWith(".vue");
                });
        } catch (Exception e) {
            log.warn("[LanguageDetector] Failed to walk for TypeScript markers, defaulting to false: {}", e.getMessage());
            return false;
        }
    }

    /**
     * Check if the project has JavaScript marker files.
     *
     * <p>判定条件（在 TS 之后判断，避免 TS 项目被识别为 JS）：
     * <ul>
     *   <li>根目录存在 {@code package.json}（且没有 tsconfig.json，已被 hasTypeScriptMarkers 先命中）</li>
     *   <li>根目录前 1000 个非排除目录文件中存在 {@code .js/.jsx} 文件</li>
     * </ul>
     */
    private static boolean hasJavaScriptMarkers(Path root) {
        if (Files.exists(root.resolve("package.json"))) {
            return true;
        }
        try (Stream<Path> walk = Files.walk(root, 10)) {
            return walk
                .filter(Files::isRegularFile)
                .filter(p -> {
                    for (Path seg : p) {
                        if (com.huawei.hisi.service.CodeAnalysisCoreService
                                .EXCLUDED_SCAN_DIRS.contains(seg.toString())) {
                            return false;
                        }
                    }
                    return true;
                })
                .limit(1000)
                .anyMatch(p -> {
                    String name = p.getFileName().toString();
                    return name.endsWith(".js") || name.endsWith(".jsx");
                });
        } catch (Exception e) {
            log.warn("[LanguageDetector] Failed to walk for JavaScript markers, defaulting to false: {}", e.getMessage());
            return false;
        }
    }

    /**
     * Count Java/Python vs TS/JS source files in the project (limit to first 1000 files to avoid performance issues).
     * 仅在各 marker 都未命中时作为兜底，按文件数判定。
     */
    private static Language countSourceFiles(Path root) {
        int javaCount = 0;
        int pythonCount = 0;
        int tsCount = 0;
        int jsCount = 0;

        try (Stream<Path> walk = Files.walk(root, 10)) {
            Set<Path> files = walk
                .filter(Files::isRegularFile)
                .filter(p -> {
                    // 排除 .worktrees / target / build / .git 等目录下的文件，避免误判语言
                    for (Path seg : p) {
                        if (com.huawei.hisi.service.CodeAnalysisCoreService
                                .EXCLUDED_SCAN_DIRS.contains(seg.toString())) {
                            return false;
                        }
                    }
                    return true;
                })
                .limit(1000)
                .collect(Collectors.toSet());

            for (Path file : files) {
                String fileName = file.getFileName().toString();
                if (fileName.endsWith(".java")) {
                    javaCount++;
                } else if (fileName.endsWith(".py")) {
                    pythonCount++;
                } else if (fileName.endsWith(".ts") || fileName.endsWith(".tsx") || fileName.endsWith(".vue")) {
                    tsCount++;
                } else if (fileName.endsWith(".js") || fileName.endsWith(".jsx")) {
                    jsCount++;
                }
            }

            log.info("[LanguageDetector] Source file counts: Java={}, Python={}, TS/JS={}",
                    javaCount, pythonCount, tsCount + jsCount);

        } catch (Exception e) {
            log.warn("[LanguageDetector] Failed to count source files, defaulting to JAVA: {}", e.getMessage());
            return Language.JAVA;
        }

        if (pythonCount > javaCount && pythonCount > tsCount + jsCount) {
            return Language.PYTHON;
        }
        if (tsCount + jsCount > javaCount && tsCount + jsCount > pythonCount) {
            return tsCount >= jsCount ? Language.TYPESCRIPT : Language.JAVASCRIPT;
        }
        return Language.JAVA; // default to Java if counts are equal or both zero
    }
}
```

---

## 二、.gitignore 漏改（远端完全没有）

**文件**：`.gitignore`

**现状**：远端 main 的 .gitignore 是**改动前版本**，缺少所有新增排除项。

**影响**：
- `sh.exe.stackdump`、`hisi-dev-tool-frontend/package-lock.json`、`codegraph.db` 等本地文件会被误提交
- 个人笔记（`2026_work_review.txt` 等）存在被误提交风险

**修复方法**：用 feat 分支版本覆盖

```bash
git checkout feat/exception-auto-fix-and-codegraph -- .gitignore
git commit -m "chore: update .gitignore with codegraph/local artifacts exclusions (missed in previous commit)"
```

**需追加到 .gitignore 的内容**：

```gitignore
# 在 OS 段追加
sh.exe.stackdump

# 在 /.codeai/ 段后追加
# Frontend build artifacts / standalone test outputs
hisi-dev-tool-frontend/package-lock.json
hisi-dev-tool-frontend/standalone-ram-test*.mjs
hisi-dev-tool-frontend/test-results-standalone/
hisi-dev-tool-frontend/.codegraph/

# Codegraph SQLite DB (local sidecar output, not source code)
**/codegraph.db
**/codegraph.db-shm
**/codegraph.db-wal

# 在文件末尾追加
# Personal notes / review files (local only)
2026_work_review.txt
HiSi-DevTool-AI4Coding.md
RAM_CHAT_FIX_PATCH.md
```

---

## 三、错改：LogQuery.vue 注释位置

**文件**：`hisi-dev-tool-frontend/src/views/log-analysis/LogQuery.vue`

**现状**：
- 远端 main：`// checkReportStatus removed...` 注释放在 `viewReportDetail` 函数后（行 1037）
- feat 分支：注释放在 `goAutoFix` 函数后（行 1053）

**影响**：🟢 低 — 无功能影响，仅注释位置不同

**修复方法**：把注释从 `viewReportDetail` 后删除，移到 `goAutoFix` 后

**远端 current（错）**：
```vue
} catch (e: any) {
    ElMessage.error('获取报告详情失败: ' + e.message)
  }
}

// checkReportStatus removed as it was never referenced in the template

const canAutoFixSelectedReport = computed(() => {
  ...
})

const goAutoFix = () => {
  ...
  router.push(`/fix/chat?reportId=${id}`)
}

const handleReanalyzeReport = async (report: any) => {
```

**feat 正确版本**：
```vue
} catch (e: any) {
    ElMessage.error('获取报告详情失败: ' + e.message)
  }
}

const canAutoFixSelectedReport = computed(() => {
  ...
})

const goAutoFix = () => {
  ...
  router.push(`/fix/chat?reportId=${id}`)
}

// checkReportStatus removed as it was never referenced in the template

const handleReanalyzeReport = async (report: any) => {
```

---

## 四、测试文件漏改（7 个）

### 4.1 FixChatServiceTest.java 漏改

**文件**：`hisi-dev-tool/src/test/java/com/huawei/hisi/fixengine/service/FixChatServiceTest.java`

**现状**：远端 main 是**改动前版本**，调用旧 3 参构造器 `new FixChatService(fixSessionRepository, agentEventRepository, fixAgent)`，与当前 `FixChatService` 5 参构造器不匹配 → **编译失败**

**修复方法**：用 feat 分支版本覆盖

```bash
git checkout feat/exception-auto-fix-and-codegraph -- hisi-dev-tool/src/test/java/com/huawei/hisi/fixengine/service/FixChatServiceTest.java
```

**关键改动**：
- 新增 import：`ObjectMapper`、`RamChatWebSocketHandler`、`timeout`
- 删除 import：`times`
- 新增字段：`@Mock RamChatWebSocketHandler wsHandler`、`ObjectMapper objectMapper`
- `setUp()` 改用 5 参构造器
- `test_happyPath` 改为异步验证：`assertThat(reply).isNull()` + `verify(..., timeout(2000).times(3))` 检查 3 个事件（USER_MSG + ASSISTANT_DELTA + CHECKPOINT）

### 4.2 Neo4jInitializerIndexTest.java 漏改

**文件**：`hisi-dev-tool/src/test/java/com/huawei/hisi/neo4j/config/Neo4jInitializerIndexTest.java`

**现状**：远端 main 是**改动前版本**，缺少 codegraph 关系索引测试

**修复方法**：用 feat 分支版本覆盖

```bash
git checkout feat/exception-auto-fix-and-codegraph -- hisi-dev-tool/src/test/java/com/huawei/hisi/neo4j/config/Neo4jInitializerIndexTest.java
```

**需追加的测试方法**：
```java
@Test
void rangeIndexes_containsCodegraphRelationshipIndexes() throws Exception {
    List<String> indexes = readListConstant(Neo4jInitializer.class, "RANGE_INDEXES");
    assertThat(indexes).anyMatch(s -> s.contains("CONTAINS") && s.contains("projectPath"));
    assertThat(indexes).anyMatch(s -> s.contains("IMPORTS") && s.contains("projectPath"));
    assertThat(indexes).anyMatch(s -> s.contains("REFERENCES") && s.contains("projectPath"));
}
```

### 4.3-4.7 五个新增测试文件完全缺失

**远端 main 完全没有以下 5 个文件**，必须新建：

| # | 文件路径 | 行数 | 测试内容 |
|---|---|---|---|
| 1 | `hisi-dev-tool/src/test/java/com/huawei/hisi/knowledgegraph/codegraph/CodegraphSidecarServiceTest.java` | 87 | Codegraph sidecar 启停、命令调用 |
| 2 | `hisi-dev-tool/src/test/java/com/huawei/hisi/knowledgegraph/codegraph/CodegraphSqliteReaderTest.java` | 191 | SQLite 读取、表结构、空 DB 处理 |
| 3 | `hisi-dev-tool/src/test/java/com/huawei/hisi/knowledgegraph/codegraph/CodegraphToNeo4jTransformerTest.java` | 302 | 节点/边转换、关系类型映射、批量入库 |
| 4 | `hisi-dev-tool/src/test/java/com/huawei/hisi/knowledgegraph/service/KnowledgeGraphBuilderCodegraphDispatchTest.java` | 185 | TS/JS 项目分流到 codegraph 路径 |
| 5 | `hisi-dev-tool/src/test/java/com/huawei/hisi/knowledgegraph/util/ProjectLanguageDetectorTest.java` | 125 | TS/JS/Vue 项目识别、Java/Python 优先级 |

**修复方法**：从 feat 分支 cherry-pick 这 5 个文件

```bash
git checkout feat/exception-auto-fix-and-codegraph -- \
  hisi-dev-tool/src/test/java/com/huawei/hisi/knowledgegraph/codegraph/CodegraphSidecarServiceTest.java \
  hisi-dev-tool/src/test/java/com/huawei/hisi/knowledgegraph/codegraph/CodegraphSqliteReaderTest.java \
  hisi-dev-tool/src/test/java/com/huawei/hisi/knowledgegraph/codegraph/CodegraphToNeo4jTransformerTest.java \
  hisi-dev-tool/src/test/java/com/huawei/hisi/knowledgegraph/service/KnowledgeGraphBuilderCodegraphDispatchTest.java \
  hisi-dev-tool/src/test/java/com/huawei/hisi/knowledgegraph/util/ProjectLanguageDetectorTest.java
```

---

## 五、修复步骤汇总（远端执行顺序）

```bash
# === 步骤 1：修复致命的 ProjectLanguageDetector 漏改 ===
git checkout feat/exception-auto-fix-and-codegraph -- \
  hisi-dev-tool/src/main/java/com/huawei/hisi/knowledgegraph/util/ProjectLanguageDetector.java

# === 步骤 2：修复 .gitignore 漏改 ===
git checkout feat/exception-auto-fix-and-codegraph -- .gitignore

# === 步骤 3：修复 LogQuery.vue 注释位置错改 ===
git checkout feat/exception-auto-fix-and-codegraph -- \
  hisi-dev-tool-frontend/src/views/log-analysis/LogQuery.vue

# === 步骤 4：修复 FixChatServiceTest 编译失败 ===
git checkout feat/exception-auto-fix-and-codegraph -- \
  hisi-dev-tool/src/test/java/com/huawei/hisi/fixengine/service/FixChatServiceTest.java

# === 步骤 5：修复 Neo4jInitializerIndexTest 漏测试 ===
git checkout feat/exception-auto-fix-and-codegraph -- \
  hisi-dev-tool/src/test/java/com/huawei/hisi/neo4j/config/Neo4jInitializerIndexTest.java

# === 步骤 6：补全 5 个完全缺失的测试文件 ===
git checkout feat/exception-auto-fix-and-codegraph -- \
  hisi-dev-tool/src/test/java/com/huawei/hisi/knowledgegraph/codegraph/CodegraphSidecarServiceTest.java \
  hisi-dev-tool/src/test/java/com/huawei/hisi/knowledgegraph/codegraph/CodegraphSqliteReaderTest.java \
  hisi-dev-tool/src/test/java/com/huawei/hisi/knowledgegraph/codegraph/CodegraphToNeo4jTransformerTest.java \
  hisi-dev-tool/src/test/java/com/huawei/hisi/knowledgegraph/service/KnowledgeGraphBuilderCodegraphDispatchTest.java \
  hisi-dev-tool/src/test/java/com/huawei/hisi/knowledgegraph/util/ProjectLanguageDetectorTest.java

# === 步骤 7：提交 ===
git add -A
git commit -m "fix: complete missing changes from feat/exception-auto-fix-and-codegraph

- ProjectLanguageDetector: complete TS/JS marker detection (114 lines missing)
- .gitignore: add codegraph/local artifacts exclusions
- LogQuery.vue: fix comment position (wrong location)
- FixChatServiceTest: update to 5-arg constructor + async verification
- Neo4jInitializerIndexTest: add codegraph relationship index test
- Add 5 missing test files for codegraph/ProjectLanguageDetector"
```

---

## 六、严重度评估

| 问题 | 严重度 | 影响 | 必须修复 |
|---|---|---|---|
| ProjectLanguageDetector 漏改 114 行 | 🔴 致命 | TS/JS 项目无法识别，codegraph sidecar 永远不被调用 | ✅ 是 |
| FixChatServiceTest 编译失败 | 🔴 致命 | `mvn test` 编译不过 | ✅ 是 |
| .gitignore 漏改 | 🟡 中 | 远端会被本地文件污染 | ✅ 是 |
| 5 个测试文件缺失 | 🟡 中 | codegraph 三件套无测试覆盖 | ✅ 是 |
| Neo4jInitializerIndexTest 漏测试 | 🟡 中 | 索引存在性无校验 | ✅ 是 |
| LogQuery.vue 注释位置 | 🟢 低 | 无功能影响 | 可选 |

---

## 七、验证清单

修复完成后，远端 main 应满足：

- [ ] `ProjectLanguageDetector.java` 共 236 行（远端当前 132 行）
- [ ] `.gitignore` 包含 `sh.exe.stackdump`、`codegraph.db`、`2026_work_review.txt` 等排除项
- [ ] `LogQuery.vue` 中 `// checkReportStatus removed` 注释在 `goAutoFix` 函数之后（非 `viewReportDetail` 之后）
- [ ] `FixChatServiceTest.java` 使用 5 参构造器，`verify(...timeout(2000)...)` 异步验证
- [ ] `Neo4jInitializerIndexTest.java` 包含 `rangeIndexes_containsCodegraphRelationshipIndexes` 测试方法
- [ ] `hisi-dev-tool/src/test/java/com/huawei/hisi/knowledgegraph/codegraph/` 目录下有 3 个测试文件
- [ ] `hisi-dev-tool/src/test/java/com/huawei/hisi/knowledgegraph/util/ProjectLanguageDetectorTest.java` 存在
- [ ] `hisi-dev-tool/src/test/java/com/huawei/hisi/knowledgegraph/service/KnowledgeGraphBuilderCodegraphDispatchTest.java` 存在
- [ ] `mvn -pl hisi-dev-tool test -Dtest='Codegraph*'` 全部通过
- [ ] `mvn -pl hisi-dev-tool test -Dtest='FixChatServiceTest'` 通过
- [ ] `mvn -pl hisi-dev-tool test -Dtest='ProjectLanguageDetectorTest'` 通过